# Secrets — NEVER commit
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Bot runtime data + logs
data/notes.db
data/notes.db-*
data/*.sqlite*
logs/

# Research datasets — kept local while we iterate, per user's "closed for now" choice.
# Switch to opt-in if/when we publish.
data/cn_training.jsonl
data/baselines.jsonl

# Editor
.DS_Store
.vscode/
.idea/
*.swp

# Dashboard build artifacts
dashboard/node_modules/
dashboard/dist/
dashboard/src/.observablehq/

# Reference PDFs (third-party content, don't redistribute)
*.pdf
